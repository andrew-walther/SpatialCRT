# Phase D 1e: settled exhibit content for the mechanic

Written by the Phase D writer (sub-step 1e), 2026-09-24. The mechanic copies this content
exactly. Nothing here needs to be recomputed. Every number's source is listed in section 4.

---

## 1. Table 2: replace the executable kable chunk

Replace the whole `{r}` chunk that builds `results_tbl` with `knitr::kable` (it starts right
after the paragraph ending "no other design had the lowest MSE in any block.") with the raw
LaTeX block below. Keep the ```` ```{=latex} ```` fence, as Tables 1 and 3–7 do. The
`\caption{}` keeps it numbered Table 2, because it is the second table float in the source.
`array` (for `>{\raggedright\arraybackslash}`) and `booktabs` are already loaded by Quarto's
template.

````
```{=latex}
\begin{table}[h]
\small\centering
\caption{Performance of the six designs at the primary scenario, $\tau = 1$, under queen contiguity (primary) and rook contiguity (sensitivity), pooled over the five incidence configurations. Each entry averages the 160 scenarios of one design and neighbor type (5 incidence configurations $\times$ 4 values of $\rho$ $\times$ 4 values of $\gamma$ $\times$ 2 spillover regimes), each summarizing 250 fits of the oracle model. Rows in both panels are in order of mean squared error (MSE) under queen contiguity. Monte Carlo standard errors of MSE, in parentheses, are computed from the 50 configuration-by-surface means (Methods); those of bias, coverage, and power are all below 0.007. Average rank is the mean of each design's within-block MSE rank among the six designs over the 160 blocks (1 = lowest MSE). Clusters treated is the mean number of clusters treated per assignment. Under rook contiguity, $\tau$ is not identified for Checkerboard ($WZ = 1 - Z$); its estimates are kept and flagged, and its coefficient on $Z$ estimates $\tau - \gamma$.}
\begin{tabular}{>{\raggedright\arraybackslash}p{4.0cm}cccccc}
\toprule
Design & MSE (SE) & Bias & Coverage & Power & \shortstack{Average\\rank} & \shortstack{Clusters\\treated} \\
\midrule
\multicolumn{7}{l}{\textit{Queen contiguity (primary)}} \\
Incidence-Guided Saturation Quadrants & 0.091 (0.0008) & 0.032 & 0.937 & 0.930 & 1.425 & 50 \\
Balanced Quartiles & 0.131 (0.0011) & 0.017 & 0.936 & 0.827 & 2.138 & 50 \\
Isolation Buffer & 0.160 (0.0011) & 0.003 & 0.939 & 0.731 & 3.694 & 21.4 \\
High Incidence Focus & 0.245 (0.0047) & 0.031 & 0.938 & 0.610 & 4.763 & 50 \\
2$\times$2 Blocking & 0.319 (0.0032) & 0.010 & 0.939 & 0.642 & 4.244 & 50 \\
Checkerboard & 1.087 (0.0101) & 0.001 & 0.941 & 0.547 & 4.737 & 50 \\
\midrule
\multicolumn{7}{l}{\textit{Rook contiguity (sensitivity)}} \\
Incidence-Guided Saturation Quadrants & 0.072 (0.0006) & 0.018 & 0.942 & 0.963 & 1.488 & 50 \\
Balanced Quartiles & 0.084 (0.0006) & 0.008 & 0.940 & 0.917 & 1.775 & 50 \\
Isolation Buffer & 0.133 (0.0009) & 0.003 & 0.939 & 0.806 & 3.381 & 38.2 \\
High Incidence Focus & 0.206 (0.0030) & 0.018 & 0.939 & 0.645 & 4.969 & 50 \\
2$\times$2 Blocking & 0.128 (0.0012) & 0.004 & 0.938 & 0.818 & 3.400 & 50 \\
Checkerboard (flagged) & 0.480 (0.0015) & $-0.652$ & 0.150 & 0.414 & 5.987 & 50 \\
\bottomrule
\end{tabular}
\end{table}
```
````

Row order: both panels use the queen-MSE order, as the brief asked. (Table 4's rook panel
uses rook-MSE order instead; the captions state the order of each table.)

---

## 2. Figures: replace the figure chunk at the end of Results

### 2a. Files

Figures 2, 3, and 4 use the files already in `paper/dissertation_chapter/figures/`.

Figure 1 should use the **ranked** version of the MSE figure. That version orders designs
best to worst and doesn't clip the rook subtitle. The alphabetical
`fig_mse_by_design_6design_{queen,rook}.pdf` files now in `figures/` show the same data in
alphabetical order, and the rook file's subtitle is clipped. Copy these two files (don't
overwrite anything):

| Source (generated by `code/14_manuscript_supplement_figures.R`) | Destination |
|---|---|
| `results/six_design_manuscript/queen/fig_mse_by_design_6design.pdf` | `paper/dissertation_chapter/figures/fig_mse_by_design_ranked_6design_queen.pdf` |
| `results/six_design_manuscript/rook/fig_mse_by_design_6design.pdf` | `paper/dissertation_chapter/figures/fig_mse_by_design_ranked_6design_rook.pdf` |

If the coordinator overrules this and keeps the alphabetical files, change one sentence in
the Figure 1 caption: replace "Designs are ordered by pooled mean MSE for the neighbor type
of the panel (Table 2)." with "Designs are listed in alphabetical order."

### 2b. Layout (one scheme for all four figures)

Each figure is one float containing two vertically stacked subfigures: (a) queen contiguity
(primary) on top and (b) rook contiguity (sensitivity) below. The prose cites them as
"Figure Na" and "Figure Nb". `subcaption` is already loaded by Quarto's template
(`\@ifpackageloaded{subcaption}{}{\usepackage{subcaption}}` in the rendered .tex); if a
render fails on `subfigure`, add `\usepackage{subcaption}` to `header-includes`.

The figures appear in this order, which matches the order of first mention in the prose:
1 MSE, 2 coverage, 3 bias–variance, 4 τ sensitivity. The current chunk has no bias–variance
figure, and it has to be added as the third float. Use `[p]` for Figure 1 (two 12 × 7 in.
panels) and `[tbp]` for the others.

Replace the whole figure chunk (from `\begin{figure}[h]` of the old MSE figure to the closing
fence, which includes the old `fig_mse_by_design_6design.pdf`,
`fig_coverage_by_design_6design.pdf`, and `fig_tau_sensitivity_6design.pdf` floats) with:

````
```{=latex}
\begin{figure}[p]
\centering
\begin{subfigure}{\linewidth}
\centering
\includegraphics[width=0.9\linewidth]{figures/fig_mse_by_design_ranked_6design_queen.pdf}
\caption{Queen contiguity (primary)}
\end{subfigure}

\vspace{0.5em}
\begin{subfigure}{\linewidth}
\centering
\includegraphics[width=0.9\linewidth]{figures/fig_mse_by_design_ranked_6design_rook.pdf}
\caption{Rook contiguity (sensitivity)}
\end{subfigure}
\caption{Mean squared error (MSE) of the oracle estimate of $\tau$ by design at $\tau = 1$, under (a) queen contiguity (primary) and (b) rook contiguity (sensitivity). Columns of panels correspond to the four levels of outcome spatial dependence $\rho$, rows to the both-arms (top) and control-only (bottom) spillover regimes, and bar colors to the spillover magnitude $\gamma$. Each bar overlays the MSEs of the five incidence configurations for one combination of design, $\rho$, $\gamma$, and regime: the bar's height is the largest of the five, and the horizontal lines within it mark the other four. Designs are ordered by pooled mean MSE for the neighbor type of the panel (Table 2). The vertical scale is set separately for each row. Under rook contiguity, $\tau$ is not identified for Checkerboard; its estimates are kept and flagged.}
\end{figure}

\begin{figure}[tbp]
\centering
\begin{subfigure}{\linewidth}
\centering
\includegraphics[width=0.62\linewidth]{figures/fig_coverage_by_design_6design_queen.pdf}
\caption{Queen contiguity (primary)}
\end{subfigure}

\vspace{0.5em}
\begin{subfigure}{\linewidth}
\centering
\includegraphics[width=0.62\linewidth]{figures/fig_coverage_by_design_6design_rook.pdf}
\caption{Rook contiguity (sensitivity)}
\end{subfigure}
\caption{Coverage of the 95\% confidence intervals by design at $\tau = 1$, under (a) queen contiguity (primary) and (b) rook contiguity (sensitivity). Each box summarizes the coverage of the 160 scenarios of one design (5 incidence configurations $\times$ 4 values of $\rho$ $\times$ 4 values of $\gamma$ $\times$ 2 spillover regimes), each estimated from 250 fits. Designs are ordered by median coverage within each panel, so the order differs between panels and from the MSE order used elsewhere. The dashed red line marks the nominal 0.95 level. Under rook contiguity, $\tau$ is not identified for Checkerboard; its estimates are kept and flagged.}
\end{figure}

\begin{figure}[tbp]
\centering
\begin{subfigure}{\linewidth}
\centering
\includegraphics[width=0.68\linewidth]{figures/fig_biasvar_6design_queen.pdf}
\caption{Queen contiguity (primary)}
\end{subfigure}

\vspace{0.5em}
\begin{subfigure}{\linewidth}
\centering
\includegraphics[width=0.68\linewidth]{figures/fig_biasvar_6design_rook.pdf}
\caption{Rook contiguity (sensitivity)}
\end{subfigure}
\caption{Decomposition of mean squared error into squared bias and variance by design at $\tau = 1$ for one incidence configuration, Poisson with $\rho_X = 0.20$, under (a) queen contiguity (primary) and (b) rook contiguity (sensitivity). For each design, the two stacked components are the squared bias and the variance ($\text{SD}^2$) of $\hat\tau$, each averaged over the 32 scenarios of that configuration (4 values of $\rho$ $\times$ 4 values of $\gamma$ $\times$ 2 spillover regimes). Designs are ordered by pooled mean MSE for the neighbor type of the panel (Table 2). The vertical scales of the two panels differ. Under rook contiguity, $\tau$ is not identified for Checkerboard; its estimates are kept and flagged, and nearly all of its MSE is squared bias.}
\end{figure}

\begin{figure}[tbp]
\centering
\begin{subfigure}{\linewidth}
\centering
\includegraphics[width=0.62\linewidth]{figures/fig_tau_sensitivity_6design_queen.pdf}
\caption{Queen contiguity (primary)}
\end{subfigure}

\vspace{0.5em}
\begin{subfigure}{\linewidth}
\centering
\includegraphics[width=0.62\linewidth]{figures/fig_tau_sensitivity_6design_rook.pdf}
\caption{Rook contiguity (sensitivity)}
\end{subfigure}
\caption{Mean squared error by design across the five values of the treatment effect, $\tau \in \{0.8, 1.0, 1.5, 2.0, 3.0\}$, under (a) queen contiguity (primary) and (b) rook contiguity (sensitivity). Each point averages the 160 scenarios of one design at one value of $\tau$, pooled over the five incidence configurations. Shaded bands show $\pm 1$ times the average of the scenario-level Monte Carlo standard errors of MSE; they describe the precision of a single scenario's MSE, not of the plotted average, whose standard error is smaller (Table 2). The five values of $\tau$ share their assignments and noise. The vertical scales of the two panels differ. Under rook contiguity, $\tau$ is not identified for Checkerboard; its estimates are kept and flagged.}
\end{figure}
```
````

After the swap, delete nothing from `figures/`. The old unsuffixed July files
(`fig_mse_by_design_6design.pdf`, `fig_coverage_by_design_6design.pdf`,
`fig_tau_sensitivity_6design.pdf`) and the alphabetical `fig_mse_by_design_6design_{queen,rook}.pdf`
files become unreferenced. Whether to remove them is the coordinator's call.

---

## 3. What each PDF actually shows (basis for the captions)

| File | Generated by | Content |
|---|---|---|
| `results/six_design_manuscript/{queen,rook}/fig_mse_by_design_6design.pdf` | 14, `plot_master_comparison()` + best-to-worst x-limits | `geom_col` with position dodged by γ only. The 5 incidence-configuration rows for each (design, γ, ρ, regime) share one x position and group, so the bars are drawn on top of one another: the visible height is the largest of the five, and the black outlines of the others appear as interior lines. Checked on the rds: queen IGSQ, ρ = 0, both arms, γ = 0.5 has configuration MSEs 0.040–0.059, and the bar top is about 0.059. Facets are ρ (columns) × regime (rows) with `scales = "free_y"`. The x axis is ordered best to worst by pooled MSE of that neighbor type. |
| `fig_coverage_by_design_6design_{queen,rook}.pdf` | 12, `plot_coverage_by_design()` | Boxplots of the 160 scenario coverages per design at τ = 1, x ordered by `reorder(Design, Coverage, FUN = median)`. Queen: 0.884–0.988 across scenarios. Rook: Checkerboard median 0.118, range 0.004–0.44. |
| `fig_biasvar_6design_{queen,rook}.pdf` | 14, `plot_bias_variance()` on the Poisson ρ_X = 0.20 slice | Stacked mean(Bias²) and mean(SD²) over that configuration's 32 scenarios at τ = 1. The x axis uses the pooled best-to-worst order of that neighbor type. Queen Checkerboard: 0.0049 + 1.11; rook Checkerboard: 0.439 + 0.042. |
| `fig_tau_sensitivity_6design_{queen,rook}.pdf` | 12, `plot_mse_vs_tau()` | Mean MSE over 160 scenarios per (design, τ). The ribbon is ± mean(SE_MSE), the average of the per-scenario surface-level SEs. For queen Checkerboard that is ±0.095, against a pooled SE of 0.010. The legend is alphabetical. |

### Figure defects the mechanic can't fix by copying (coordinator decision)

1. `fig_coverage_by_design_6design_rook.pdf`: the subtitle is clipped at the right edge
   (it breaks off at "Checkerboard (WZ = 1 − Z").
2. `fig_tau_sensitivity_6design_rook.pdf`: the ggplot caption is clipped at the left edge
   (it begins mid-word, "ot identified for Checkerboard ...").
3. All figures have in-plot titles and subtitles in plain text ("tau=1.0", "rho = 0.2",
   "rho_X = 0.20"). A dissertation figure usually carries no in-plot title.
4. Figure 1's overlaid bars (see the table above) show only the largest of five
   configuration MSEs. The caption now describes this accurately, but a regenerated figure
   with averaged bars would be easier to read.

The chapter caption carries the rook non-identification note, so the figures stay accurate
as they are. Fixing 1–3 means regenerating from `code/12_…` and `code/14_…` (for example,
wrapping `ROOK_NOTE` or dropping in-plot titles). That is a code change outside this
sub-step.

---

## 4. Sources for every number in Table 2 and the captions

- MSE, Coverage, Power, Mean_Treated (queen and rook): `dissertation_results_extract.txt` §1
  (Overall performance, τ = 1.0). Queen IGSQ 0.09067 / 0.93655 / 0.930275 / 50; BQ 0.13081 /
  0.93595 / 0.827275 / 50; IB 0.16016 / 0.939 / 0.7306 / 21.43; HIF 0.24546 / 0.93785 / 0.6099 / 50;
  2x2 0.31923 / 0.939075 / 0.64205 / 50; CB 1.08690 / 0.940725 / 0.5471 / 50. Rook IGSQ 0.07165 /
  0.9423 / 0.962675 / 50; BQ 0.08372 / 0.94015 / 0.9173 / 50; 2x2 0.12813 / 0.937825 / 0.817725 / 50;
  IB 0.13290 / 0.9394 / 0.806025 / 38.25; HIF 0.20624 / 0.939125 / 0.64475 / 50; CB 0.47992 /
  0.149775 / 0.41365 / 50.
- Bias: `dissertation_results_extract.txt` §10 (Bias.oracle). Queen 0.03212, 0.01727, 0.00262,
  0.03127, 0.00959, 0.00113. Rook 0.01849, 0.00848, 0.00397 (2x2), 0.00251 (IB), 0.01772,
  −0.65185.
- Average rank: `six_design_summary.txt`, "Average ranks", True_Tau = 1.0. Queen 1.425,
  2.138, 3.694, 4.244, 4.737, 4.763. Rook 1.488, 1.775, 3.381, 3.400, 4.969, 5.987.
- MSE SEs (in parentheses) and the "below 0.007" bound: computed on
  `results/sim_data/surface_results_MLE_tau_sweep_20260924_025509.rds` (τ = 1). For each
  (neighbor type, design, configuration, surface), average the surface-level MSE over the
  32 blocks, giving 50 units; SE = sd/√50. Queen 0.000816, 0.001147, 0.001116, 0.004658,
  0.003188, 0.010107. Rook 0.000561, 0.000644, 0.000915 (IB), 0.003008, 0.001243 (2x2),
  0.001455. The 50-unit means reproduce the pooled MSEs exactly. The largest SE of bias is
  0.00611 (queen Checkerboard), of power 0.00613 (queen High Incidence Focus), and of
  coverage 0.00161.
- The caption's "160 scenarios", "250 fits", "32 scenarios", "40 / 80 scenarios": the
  parameter grid (Methods; `sim_results_MLE_tau_sweep_combined_20260924_025509.rds` has
  160 rows per design × neighbor type at τ = 1, all N_Valid_Est = 250).
- Figure 3 "nearly all of its MSE is squared bias" (rook Checkerboard, Poisson ρ_X = 0.20):
  0.439 of 0.481, computed on the combined rds.
- Figure 4 "standard error [of the average] is smaller": mean scenario SE_MSE (for example,
  queen Checkerboard 0.095 at τ = 1, combined rds) against the pooled SE in Table 2 (0.0101).
